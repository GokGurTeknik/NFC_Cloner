name: nfc_cloner
description: "NFC Kart Klonlayıcı - Kartını oku ve telefonuna kaydet"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  nfc_manager: ^3.3.0
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
